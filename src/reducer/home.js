/**
 * Created by admin on 11/9/16.
 */
import * as actionType from '../application/ActionType';

const initialState = {
};

export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}